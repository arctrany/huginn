# Huginn
- Backend CLI: HTTP REST API & Rails runner
- Native Format: JSON (Scenarios), RDBMS Database mappings
- Use case: Generate agent control JSON requests -> Huginn API handles state -> Returns output

Important: Huginn backend may require authentication and will execute remotely or on the Dockerized localhost.
